apiVersion: v1
kind: Service
metadata:
  name: ticketing-app-service
  labels:
    app: ticketing-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: ticketing-app