apiVersion: v1
kind: Service
metadata:
  name: manager-service
  labels:
    app: manager
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
  selector:
    app: manager